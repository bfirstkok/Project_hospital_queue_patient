# 🎓 คู่มือติวเข้ม 100 คำถาม-แนวคำตอบ สำหรับสอบป้องกันโปรเจกต์ (Thesis Defense Q&A Guide)
**โครงการ:** ระบบจองและตรวจสอบสถานะคิวผู้ป่วยนอก (OPD Smart Hospital - Patient Queue & Medical Portal)  
**เทคโนโลยีหลัก:** Next.js 16 (App Router / Static Export), React 19, TypeScript, Playwright, Python Mock Backend

---

## 📌 สารบัญหมวดคำถาม
1. [หมวดที่ 1: สถาปัตยกรรมระบบและเทคโนโลยีหลัก (Architecture & Stack) [ข้อ 1 - 15]](#หมวดที่-1-สถาปัตยกรรมระบบและเทคโนโลยีหลัก-ข้อ-1---15)
2. [หมวดที่ 2: การควบคุมหน้าจอและการนำทาง (State Routing & Navigation) [ข้อ 16 - 28]](#หมวดที่-2-การควบคุมหน้าจอและการนำทาง-ข้อ-16---28)
3. [หมวดที่ 3: ระบบยืนยันตัวตน, PIN 6 หลัก และความปลอดภัย (Auth, PIN & Security) [ข้อ 29 - 45]](#หมวดที่-3-ระบบยืนยันตัวตน-pin-6-หลัก-และความปลอดภัย-ข้อ-29---45)
4. [หมวดที่ 4: ระบบคิวผู้ป่วย, การคำนวณเวลา และการแจ้งเตือน (Queue Lifecycle) [ข้อ 46 - 63]](#หมวดที่-4-ระบบคิวผู้ป่วย-การคำนวณเวลา-และการแจ้งเตือน-ข้อ-46---63)
5. [หมวดที่ 5: การลงทะเบียน, PDPA และเวชระเบียน (Registration, PDPA & Profile) [ข้อ 64 - 76]](#หมวดที่-5-การลงทะเบียน-pdpa-และเวชระเบียน-ข้อ-64---76)
6. [หมวดที่ 6: การออกแบบเพื่อผู้สูงอายุและ Responsive UX (Senior & Accessibility) [ข้อ 77 - 85]](#หมวดที่-6-การออกแบบเพื่อผู้สูงอายุและ-responsive-ux-ข้อ-77---85)
7. [หมวดที่ 7: การทดสอบอัตโนมัติและการส่งมอบระบบ (Testing & Deployment) [ข้อ 86 - 93]](#หมวดที่-7-การทดสอบอัตโนมัติและการส่งมอบระบบ-ข้อ-86---93)
8. [หมวดที่ 8: คำถามจำลองสถานการณ์ "สั่งแก้โค้ดสด / ย้ายปุ่ม / ปรับเงื่อนไข" [ข้อ 94 - 100]](#หมวดที่-8-คำถามจำลองสถานการณ์-สั่งแก้โค้ดสด--ย้ายปุ่ม--ปรับเงื่อนไข-ข้อ-94---100)

---

## หมวดที่ 1: สถาปัตยกรรมระบบและเทคโนโลยีหลัก (ข้อ 1 - 15)

### ข้อ 1: สถาปัตยกรรมโดยรวมของโปรเจกต์นี้เป็นแบบใด?
* **ไฟล์ที่เกี่ยวข้อง:** `README.md`, `src/app/page.tsx`, `package.json`
* **แนวคำตอบ:** เป็น **Client-side Single Page Application (SPA)** พัฒนาด้วย **Next.js 16 (React 19, TypeScript)** โดยตั้งค่าเป็น **Static Export** (`output: 'export'`) ผลลัพธ์จากการ Build จะได้ไฟล์ HTML/CSS/JavaScript บริสุทธิ์ในโฟลเดอร์ `dist/` และติดต่อกับเซิร์ฟเวอร์โรงพยาบาลผ่าน **REST API**
* **ข้อควรระวัง:** อย่าบอกว่าเป็น Server-Side Rendering (SSR) เต็มรูปแบบ เพราะโปรเจกต์นี้ Export เป็น Static เพื่อให้ Deploy บน Web Server ทั่วไปได้ง่าย

### ข้อ 2: ทำไมจึงเลือกใช้ Next.js 16 ทั้งที่ทำ Static Export? ทำไมไม่ใช้ Vite?
* **ไฟล์ที่เกี่ยวข้อง:** `next.config.ts`, `package.json`
* **แนวคำตอบ:** Next.js 16 ให้โครงสร้างโปรเจกต์ที่ได้มาตรฐานระดับองค์กร มีระบบ App Router, Image Optimization, และ Bundle Analyzer ในตัว และที่สำคัญคือรองรับการขยายระบบในอนาคต หากโรงพยาบาลต้องการเปิดใช้ฟีเจอร์ Server-Side เช่น Server Actions หรือ API Gateway ก็สามารถปรับ Config ได้โดยไม่ต้องย้ายโปรเจกต์

### ข้อ 3: คำว่า `basePath: '/patient'` ใน `next.config.ts` มีไว้เพื่ออะไร?
* **ไฟล์ที่เกี่ยวข้อง:** `next.config.ts`
* **แนวคำตอบ:** มีไว้เพื่อให้ระบบสามารถนำไป Deploy ร่วมกับระบบอื่นๆ ของโรงพยาบาลภายใต้โดเมนเดียวกันได้ เช่น `hospital.com/patient` โดยไม่ชนกับระบบแอดมินหรือระบบแพทย์ที่อาจจะอยู่ที่ `/doctor` หรือ `/admin`

### ข้อ 4: โปรเจกต์นี้แบ่งโครงสร้างโฟลเดอร์ตามหลักการใด?
* **ไฟล์ที่เกี่ยวข้อง:** โฟลเดอร์ `src/` (`app/`, `features/`, `shared/`)
* **แนวคำตอบ:** ใช้รูปแบบ **Feature-based Architecture (หรือ Domain-driven layout)** แยกตามฟังก์ชันธุรกิจชัดเจน ได้แก่ `features/auth`, `features/queue`, `features/registration`, `features/account`, `features/settings` และรวมคอมโพเนนต์ส่วนกลางไว้ที่ `shared/` ทำให้โปรเจกต์บำรุงรักษาง่ายและไม่ปนกัน

### ข้อ 5: ระบบนี้ติดต่อกับ Backend ผ่านโปรโตคอลใด?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/api/patient-api.ts`
* **แนวคำตอบ:** ติดต่อผ่านโปรโตคอล **HTTP/HTTPS ในรูปแบบ RESTful API** โดยส่งและรับข้อมูลเป็นรูปแบบ JSON และส่งรหัส Token ผ่าน HTTP Header `Authorization: Bearer <token>`

### ข้อ 6: ไฟล์ `mock_backend.py` มีไว้ทำหน้าที่อะไรในระบบ?
* **ไฟล์ที่เกี่ยวข้อง:** `mock_backend.py`
* **แนวคำตอบ:** เป็น Local HTTP Mock Server เขียนด้วยภาษา Python เพื่อจำลอง Endpoint ทั้งหมดของโรงพยาบาล (เช่น การ Login, ตรวจสอบคิว, การเรียกประวัติการรักษา) ให้นักพัฒนาสามารถพัฒนาหน้าบ้านและรันชุดทดสอบ Playwright E2E ได้อย่างสมบูรณ์โดยไม่ต้องพึ่งพาเซิร์ฟเวอร์จริงตลอดเวลา

### ข้อ 7: ในสภาพแวดล้อมจริง (Production) ระบบเปลี่ยน URL ของ Backend ได้อย่างไรโดยไม่ต้อง Build โค้ดใหม่?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/config/runtime-config.ts`, `public/runtime-config.js`
* **แนวคำตอบ:** ใช้เทคนิค **Runtime Configuration Injection** โดยอ่านค่าจากตัวแปร `window.PATIENT_APP_ENV.apiBaseUrl` ซึ่งถูกโหลดผ่านไฟล์ `runtime-config.js` ทำให้ผู้ดูแลระบบสามารถเปลี่ยน IP/Domain ของ Backend ได้ทันทีที่ไฟล์นี้ โดยไม่ต้องสั่ง `npm run build` ใหม่

### ข้อ 8: ถ้าเครื่องแม่ข่ายโรงพยาบาลไม่มี Node.js รันเว็บนี้ได้หรือไม่?
* **ไฟล์ที่เกี่ยวข้อง:** โฟลเดอร์ `dist/`, `scripts/publish-export.mjs`
* **แนวคำตอบ:** รันได้ 100% ครับ เพราะระบบถูกคอมไพล์ผ่าน `npm run build` ออกมาเป็นไฟล์ Static Files (.html, .js, .css) ในโฟลเดอร์ `dist/` แล้ว สามารถนำไปวางบน Nginx, Apache, IIS หรือแม้แต่ Python HTTP Server ได้ทันที

### ข้อ 9: ทำไมถึงเลือก TypeScript แทนที่จะเป็น JavaScript ธรรมดา?
* **ไฟล์ที่เกี่ยวข้อง:** `tsconfig.json`, `src/shared/api/types.ts`
* **แนวคำตอบ:** ช่วยตรวจจับข้อผิดพลาดตั้งแต่ตอนเขียนโค้ด (Compile-time type checking) โดยเฉพาะในระบบโรงพยาบาลที่มีโครงสร้างข้อมูลคนไข้ซับซ้อน เช่น ฟิลด์ประวัติการรักษา, สัญญาณชีพ, สถานะคิว การใช้ TypeScript Interface ช่วยป้องกันปัญหา Property Undefined ได้อย่างเด็ดขาด

### ข้อ 10: สคริปต์ `npm run build` มีขั้นตอนการทำงานอย่างไรบ้าง?
* **ไฟล์ที่เกี่ยวข้อง:** `package.json` (scripts.build)
* **แนวคำตอบ:** ประกอบด้วย 4 ขั้นตอนต่อเนื่อง:
  1. `clean-build.mjs`: ลบโฟลเดอร์ build เก่าทิ้ง
  2. `write-runtime-config.mjs`: สร้าง runtime-config.js จาก .env
  3. `next build`: สั่ง Next.js คอมไพล์และ Export เป็น Static HTML
  4. `publish-export.mjs`: ย้ายไฟล์ไปที่ `dist/` และสร้าง index.html สำหรับทำ auto-redirect

### ข้อ 11: `public/` กับ `dist/` แตกต่างกันอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `public/`, `dist/`
* **แนวคำตอบ:** `public/` เป็นที่เก็บ Static Assets ดิบระหว่างการพัฒนา (เช่น รูปภาพ, ไอคอน) ส่วน `dist/` คือผลลัพธ์สุดท้ายหลังผ่านการคอมไพล์ ย่อขนาดไฟล์ (Minify) และจัดโครงสร้างพร้อมสำหรับนำขึ้น Production Server

### ข้อ 12: ระบบมีการจัดการ Error ในภาพรวมของ UI อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/ui/ErrorBoundary.tsx`
* **แนวคำตอบ:** มีการครอบคอมโพเนนต์ด้วย **React Error Boundary** (`ErrorBoundary.tsx`) เพื่อดักจับ JavaScript Runtime Errors หากหน้าจอย่อยเกิดพัง ระบบจะไม่แครชขาวทั้งหน้า แต่จะแสดงข้อความแจ้งเตือนที่สุภาพพร้อมปุ่มรีเฟรชหน้าจอ

### ข้อ 13: ระบบทราบได้อย่างไรว่าอุปกรณ์ของผู้ใช้ Offline (เน็ตหลุด)?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/ui/OfflineBanner.tsx`
* **แนวคำตอบ:** ดักฟังสัญญาณ Browser Event `window.addEventListener('online')` และ `'offline'` ในคอมโพเนนต์ `OfflineBanner.tsx` เมื่อสัญญาณเน็ตขาดหาย แถบแจ้งเตือนสีส้มจะปรากฏขึ้นบนหน้าจอทันทีโดยไม่ต้องรอให้ยิง API ล้มเหลว

### ข้อ 14: โค้ดส่วนใหญ่ในโปรเจกต์มีคำสั่ง `"use client";` อยู่ด้านบนสุด หมายความว่าอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`, ไฟล์ใน `features/`
* **แนวคำตอบ:** เป็น Directive ของ Next.js App Router เพื่อระบุว่าคอมโพเนนต์นี้ต้องถูก Execute บนฝั่ง Client (เบราว์เซอร์) เท่านั้น ซึ่งจำเป็นต่อการใช้งาน React Hook เช่น `useState`, `useEffect`, และ Browser APIs เช่น `localStorage`, `window`

### ข้อ 15: มีการป้องกันเรื่อง Memory Leak ในคอมโพเนนต์ต่างๆ อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueuePolling.ts`, `src/features/queue/useQueueNotification.ts`
* **แนวคำตอบ:** ทุกครั้งที่มีการสร้าง Timer (`setInterval`, `setTimeout`) หรือ AudioContext ระบบจะใส่ฟังก์ชัน Cleanup ใน `return () => { clearInterval(timer); }` ของ `useEffect` เสมอ เพื่อคืนหน่วยความจำเมื่อผู้ใช้เปลี่ยนหน้า

---

## หมวดที่ 2: การควบคุมหน้าจอและการนำทาง (ข้อ 16 - 28)

### ข้อ 16: ทำไมระบบถึงไม่ใช้ Next.js File-based routing (เช่น `/login`, `/queue`) แต่ใช้ State-based view ใน `page.tsx`?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`
* **แนวคำตอบ:** เพราะต้องการให้เป็น Web Application เสมือนโมบายแอปพลิเคชัน (App-like experience) การเปลี่ยนหน้าผ่าน State (`view`) ทำได้รวดเร็วทันทีโดยไม่ต้องโหลดไฟล์ HTML ใหม่ และช่วยให้การควบคุม Security Guard (เช่น ตรวจสอบ PIN/Session) ทำได้เบ็ดเสร็จในจุดเดียวที่ root component

### ข้อ 17: ตัวแปร `view` ใน `src/app/page.tsx` รองรับสถานะหน้าจออะไรบ้าง?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx` (Type `View`)
* **แนวคำตอบ:** มีทั้งหมด 9 หน้าจอ: `login`, `pin_unlock`, `pin_setup`, `pin_change`, `pin_reset`, `status`, `registration`, `account`, `settings`

### ข้อ 18: เมื่อผู้ใช้กดรีเฟรชหน้าเบราว์เซอร์ (F5) ระบบรู้ได้อย่างไรว่าต้องเปิดหน้าเดิม?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`
* **แนวคำตอบ:** ระบบบันทึกชื่อหน้าจอปัจจุบันลงใน `localStorage` ภายใต้คีย์ `patient_app_current_view` เมื่อรีเฟรช หน้าเว็บจะอ่านคีย์นี้ขึ้นมาแสดงผลร่วมกับการตรวจสอบ Token และสถานะปลดล็อก PIN

### ข้อ 19: ถ้าผู้ใช้ยังไม่ได้ล็อกอิน แต่พยายามเปิดหน้า `status` ระบบมีกลไกป้องกันอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx` (ใน `useEffect` ตรวจสอบ Token)
* **แนวคำตอบ:** มี Route Guard ใน `useEffect` หากพบว่าไม่มี `token` และไม่ได้อยู่ในขั้นตอนยืนยันตัวตน ระบบจะบังคับ `setView("login")` ทันที

### ข้อ 20: ส่วนของ Navigation Bar ถูกแยกแสดงผลตามอุปกรณ์อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/ui/SiteShell.tsx`, `src/shared/ui/AppNavbar.tsx`
* **แนวคำตอบ:** ใช้ Responsive CSS และ Media Queries:
  * บน **Mobile**: แสดงผลเป็น **Bottom Navigation Bar** แปะติดขอบล่างหน้าจอ เพื่อให้ใช้นิ้วโป้งแตะได้สะดวก
  * บน **Desktop**: แสดงผลเป็น **Top Header Bar** ด้านบนขวา

### ข้อ 21: เมนูในแถบนำทาง (Navbar) มีปุ่มอะไรบ้าง?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/ui/AppNavbar.tsx`
* **แนวคำตอบ:** มี 4 ปุ่มหลัก:
  1. บัตรคิว (Queue Status)
  2. ลงทะเบียน/ตรวจรักษา (Registration)
  3. บัญชีผู้ป่วย (Account/Records)
  4. ตั้งค่า (Settings)

### ข้อ 22: ถ้าผู้ใช้กำลังอยู่ในคิวตรวจ (Queue Active) แล้วกดไปหน้าอื่น จะดูคิวต่อได้อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`, `src/shared/ui/SiteShell.tsx`
* **แนวคำตอบ:** สามารถกดปุ่มเมนู "บัตรคิว" บน Navbar เพื่อสลับกลับมาหน้าคิวได้ตลอดเวลา และระบบมีตัวแปร `queueActive` บันทึกสถานะไว้ ทำให้ไม่ต้องจองคิวใหม่

### ข้อ 23: คอมโพเนนต์ `SiteShell` มีบทบาทอะไรในโครงสร้างหน้าเว็บ?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/ui/SiteShell.tsx`
* **แนวคำตอบ:** เป็น Master Layout ครอบทุกหน้าจอ ทำหน้าที่จัดวางโครงสร้างหลัก (Header, Main Container, Bottom Bar), ควบคุมระดับขนาดตัวอักษรของทั้งระบบ (`data-font-size`), และแสดงผลแจ้งเตือนสถานะ Offline

### ข้อ 24: เมื่อผู้ใช้กด "ออกจากระบบ" (Logout) ระบบทำอะไรบ้าง?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx` (ฟังก์ชัน `resetToLogin`)
* **แนวคำตอบ:** 
  1. ลบ Token ออกจาก Storage (`clearToken()`)
  2. ลบ Session Unlocked ออกจาก `sessionStorage`
  3. เคลียร์ State คิวและประวัติในหน่วยความจำ
  4. ปรับ `view` กลับไปที่หน้า `login`

### ข้อ 25: ฟังก์ชัน `setPinSetupReturnView` มีไว้ทำหน้าที่อะไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`
* **แนวคำตอบ:** มีไว้จำว่าเมื่อผู้ใช้ตั้งรหัส PIN เสร็จเรียบร้อยแล้ว จะต้องพากลับไปยังหน้าจอใด (เช่น หากมาจากหน้า Login ให้ส่งไปหน้าบัตรคิว `status` แต่ถ้ามาจากหน้า Settings ให้ส่งกลับไปหน้า `settings`)

### ข้อ 26: ทำไมระบบถึง Scroll หน้าจอกลับไปบนสุดเสมอเมื่อเปลี่ยนหน้า?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`
* **แนวคำตอบ:** มีคำสั่ง `window.scrollTo({ top: 0, behavior: "smooth" })` ใน `useEffect` ของการสลับหน้าจอ เพื่อไม่ให้ค้างตำแหน่งการเลื่อนเดิมจากหน้าก่อนหน้า สร้าง UX ที่เป็นธรรมชาติสำหรับผู้ใช้งานมือถือ

### ข้อ 27: ฟังก์ชัน `LoadingScreen` จะถูกเรียกทำงานในจังหวะไหน?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`, `src/shared/ui/LoadingScreen.tsx`
* **แนวคำตอบ:** จะแสดงผลในช่วงที่เว็บเพิ่งเปิดขึ้นมาและตัวแปร `initialized` ยังเป็น `false` เพื่อรอให้ระบบตรวจสอบความถูกต้องของ Token และ Session ใน LocalStorage ให้เสร็จก่อนเรนเดอร์หน้าจอจริง

### ข้อ 28: ถ้าต้องการซ่อน Navbar ในหน้า Login และหน้ากรอก PIN ทำได้อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/ui/SiteShell.tsx`, `src/app/page.tsx`
* **แนวคำตอบ:** ใน `page.tsx` มีการเช็คเงื่อนไข `view` หากเป็น `login` หรือขึ้นต้นด้วย `pin_` จะไม่ส่งฟังก์ชันการสลับหน้าจอเข้าไป หรือส่งค่าซ่อน Navbar ไปยัง `SiteShell`

---

## หมวดที่ 3: ระบบยืนยันตัวตน, PIN 6 หลัก และความปลอดภัย (ข้อ 29 - 45)

### ข้อ 29: ระบบรองรับการเข้าสู่ระบบกี่รูปแบบ?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/LoginView.tsx`
* **แนวคำตอบ:** รองรับ 3 รูปแบบ:
  1. เข้าสู่ระบบด้วย **Username หรือ เลขประจำตัวประชาชน 13 หลัก** คู่กับรหัสผ่าน
  2. เข้าสู่ระบบด้วย **Social Login (Google OAuth)**
  3. การกู้คืนรหัสผ่านด้วยรหัส **OTP ทางอีเมล/เบอร์โทรศัพท์**

### ข้อ 30: ทำไมต้องมีทั้งรหัสผ่าน (Password) และรหัส PIN 6 หลัก?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/LoginView.tsx`, `src/features/auth/PinAuthView.tsx`
* **แนวคำตอบ:** เป็นการรักษาความปลอดภัยแบบ **Two-Layered Defense**:
  * **Password**: ใช้ยืนยันตัวตนในระดับบัญชีผู้ใช้เมื่อเข้าใช้งานครั้งแรก (Authentication)
  * **PIN 6 หลัก**: ใช้ปกป้องข้อมูลเวชระเบียนสุขภาพ (Medical Records) ในกรณีที่ผู้ป่วยเปิดเว็บค้างไว้บนมือถือ แล้ววางทิ้งไว้ เมื่อกลับมาใช้งานจะต้องยืนยัน PIN อย่างรวดเร็ว ป้องกันผู้อื่นแอบดูข้อมูลสุขภาพ

### ข้อ 31: รหัส Token ถูกเก็บไว้ที่ไหน และเพราะเหตุใด?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/auth/token-storage.ts`
* **แนวคำตอบ:** เก็บไว้ใน `localStorage` เพื่อให้ผู้ป่วยไม่ต้องกรอก Username/Password ใหม่ทุกครั้งที่เปิดเบราว์เซอร์ขึ้นมาใหม่

### ข้อ 32: สถานะการปลดล็อก PIN ถูกเก็บไว้ที่ไหน แตกต่างจาก Token อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`, `src/shared/auth/pin-storage.ts`
* **แนวคำตอบ:** สถานะปลดล็อก PIN ถูกเก็บไว้ใน `sessionStorage` (`patient_session_unlocked = 'true'`) แตกต่างจาก Token ตรงที่ **เมื่อผู้ป่วยปิดแท็บเบราว์เซอร์ ค่าใน sessionStorage จะถูกลบทันที** ทำให้เมื่อเปิดกลับเข้ามาใหม่จะต้องใส่ PIN ซ้ำเพื่อความปลอดภัย

### ข้อ 33: แป้นพิมพ์ PIN จำลอง (Virtual Keypad) มีประโยชน์อย่างไรในแง่ความปลอดภัย?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/PinAuthView.tsx`
* **แนวคำตอบ:** ป้องกันมัลแวร์ประเภท **Keylogger** หรือการดักจับสัญญาณแป้นพิมพ์จริงของระบบปฏิบัติการ และปุ่มถูกออกแบบให้มีขนาดใหญ่กว่า 48px เหมาะกับหน้าจอสัมผัสของผู้สูงอายุ

### ข้อ 34: หากกรอก PIN ผิด ระบบมีระบบป้องกันการสุ่มรหัส (Brute-Force Attack) อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/auth/pin-storage.ts`, `src/features/auth/PinAuthView.tsx`
* **แนวคำตอบ:** ระบบมีการนับจำนวนครั้งที่ผิด หากกรอกผิดเกินกำหนด (เช่น 5 ครั้ง) ระบบจะสั่งล็อคการกรอก PIN ชั่วคราวตามเวลาที่กำหนด (Lockout timer) หรือบังคับให้ผู้ใช้ต้องล็อกอินใหม่ด้วย Password หลัก

### ข้อ 35: การตั้งรหัส PIN ครั้งแรกมีขั้นตอนยืนยันอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/PinAuthView.tsx`
* **แนวคำตอบ:** มีขั้นตอน **2-Step Verification**:
  1. ขั้นที่ 1: กำหนดรหัส PIN 6 หลักที่ต้องการ
  2. ขั้นที่ 2: ยืนยันรหัส PIN 6 หลักเดิมอีกครั้ง เพื่อป้องกันการกดผิด หากรหัสทั้งสองรอบตรงกัน ระบบจึงจะบันทึกและให้เข้าใช้งาน

### ข้อ 36: การตรวจสอบความถูกต้องของรหัส PIN ทำที่ฝั่ง Client หรือ Server?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/auth/pin-storage.ts`, `mock_backend.py`
* **แนวคำตอบ:** สถาปัตยกรรมรองรับทั้งสองระดับ: ในโหมด Offline/Client จะตรวจสอบผ่านการแฮช (Hash & Salt) ที่จัดเก็บใน Local Storage ที่เข้ารหัสไว้ และส่งคำขอยืนยันไปยัง Endpoint `/api/patient/pin/verify` ของ Backend เพื่อตรวจสอบความถูกต้องร่วมด้วย

### ข้อ 37: หากผู้ป่วยลืมรหัส PIN มีกระบวนการแก้ไขอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/PinAuthView.tsx`
* **แนวคำตอบ:** ผู้ป่วยสามารถกดปุ่ม "ลืมรหัส PIN" ระบบจะพากลับไปยืนยันตัวตนด้วยรหัสผ่านหลัก (Password) หรือรหัส OTP ทางอีเมล เมื่อยืนยันตัวตนผ่านแล้ว จึงจะอนุญาตให้ตั้งค่ารหัส PIN 6 หลักใหม่ได้

### ข้อ 38: ฟังก์ชันลืมรหัสผ่าน (Forgot Password) ทำงานอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/ForgotPasswordModal.tsx`
* **แนวคำตอบ:** มี 3 สเต็ป:
  1. ผู้ใช้กรอก Username หรือเลขบัตรประชาชน
  2. ระบบส่งรหัส OTP 6 หลักไปยังอีเมลหรือ SMS ที่ผูกไว้
  3. ผู้ใช้กรอก OTP และตั้งรหัสผ่านใหม่

### ข้อ 39: ในชุดทดสอบและเดโม รหัส OTP ที่ใช้ทดสอบคือเลขอะไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/ForgotPasswordModal.tsx`, `mock_backend.py`, `README.md`
* **แนวคำตอบ:** คือเลข **`123456`**

### ข้อ 40: ระบบป้องกันการดักจับรหัสผ่านขณะพิมพ์ (Shoulder Surfing) อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/LoginView.tsx`
* **แนวคำตอบ:** ฟิลด์รหัสผ่านถูกกำหนดประเภทเป็น `type="password"` เป็นค่าเริ่มต้น โดยมีปุ่มสลับการมองเห็น (Password Visibility Toggle) ให้ผู้ใช้เลือกเปิดดูได้เฉพาะเมื่อจำเป็น

### ข้อ 41: การส่ง Token ไปกับ API Request ทำอย่างไรใน `patient-api.ts`?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/api/patient-api.ts`
* **แนวคำตอบ:** ในฟังก์ชัน Fetch ส่วนกลาง จะตรวจสอบว่ามี `token` ส่งเข้ามาหรือไม่ หากมี จะแนบ Header:
  ```typescript
  headers: {
    "Authorization": `Bearer ${token}`,
    "Content-Type": "application/json"
  }
  ```

### ข้อ 42: หาก Token หมดอายุ (HTTP 401 Unauthorized) ระบบจัดการอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueuePolling.ts`, `src/shared/api/patient-api.ts`
* **แนวคำตอบ:** ในตัวดักจับ Response ของ API เมื่อได้รับ Status 401 ระบบจะเรียกฟังก์ชัน `onUnauthorized()` เพื่อทำการเคลียร์ Token ในเครื่อง และส่งผู้ป่วยกลับไปหน้า `login` ทันทีเพื่อความปลอดภัย

### ข้อ 43: รหัสผ่านของบัญชีทดสอบเดโมคืออะไร?
* **ไฟล์ที่เกี่ยวข้อง:** `README.md`, `mock_backend.py`
* **แนวคำตอบ:** 
  * **Username:** `somchai99`
  * **Password:** `Password@2026`

### ข้อ 44: ระบบมีการใช้ Web API ตัวใดช่วยเพิ่มการตอบสนองเมื่อกดปุ่มบนมือถือ?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/PinAuthView.tsx`
* **แนวคำตอบ:** ใช้ `navigator.vibrate(10)` เพื่อสร้างแรงสั่นเบาๆ (Haptic Feedback) ให้ความรู้สึกเหมือนกำลังกดแป้นพิมพ์จริงบนมือถือ

### ข้อ 45: หากอุปกรณ์ไม่รองรับคำสั่ง `navigator.vibrate` โค้ดจะพังหรือไม่?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/PinAuthView.tsx`, `src/features/queue/useQueueNotification.ts`
* **แนวคำตอบ:** ไม่พังครับ เพราะมีการเช็คเงื่อนไขความปลอดภัยก่อนเสมอ: `if (typeof window !== "undefined" && "vibrate" in navigator) { navigator.vibrate(...); }`

---

## หมวดที่ 4: ระบบคิวผู้ป่วย, การคำนวณเวลา และการแจ้งเตือน (ข้อ 46 - 63)

### ข้อ 46: ระบบอัปเดตสถานะคิวแบบ Real-time โดยใช้เทคนิคใด?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueuePolling.ts`
* **แนวคำตอบ:** ใช้เทคนิค **Short Polling** ผ่าน `window.setInterval` ยิงไปที่ API `/api/patient/queue` ทุกๆ 10 วินาที (ค่าตั้งตาม `statusRefreshMs` ใน config)

### ข้อ 47: ทำไมถึงเลือกใช้ Polling แทนที่จะใช้ WebSocket?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueuePolling.ts`, `README.md`
* **แนวคำตอบ:** 
  1. ความถี่ 10 วินาทีเพียงพอสำหรับบริบทคิวตรวจคนไข้ ซึ่งรอบการเรียกตรวจมักใช้เวลาเป็นนาที
  2. โครงสร้างระบบแบบ Static Export ทำงานร่วมกับ REST API ได้เสถียรที่สุด
  3. ไม่ต้องเปลืองทรัพยากรการเปิด Connection ค้างไว้บนเซิร์ฟเวอร์โรงพยาบาล

### ข้อ 48: การ Polling ทุก 10 วินาที จะทำให้หน้าจอผู้ป่วยกระพริบหรือไม่?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueuePolling.ts`
* **แนวคำตอบ:** ไม่กระพริบครับ เพราะระบบใช้เทคนิค **Background Silent Refresh** โดยส่งแฟล็ก `silent = true` ในการดึงข้อมูลรอบต่อๆ ไป ทำให้ตัวหมุน Loading ไม่ถูกเปิดขึ้นมาขัดจังหวะสายตาผู้ใช้

### ข้อ 49: ในหน้าบัตรคิว (`QueueStatusView`) มีข้อมูลสำคัญอะไรแสดงบ้าง?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/QueueStatusView.tsx`
* **แนวคำตอบ:** แสดง:
  1. หมายเลขคิว (เช่น A012)
  2. สถานะคิว (รอตรวจ, กำลังตรวจ, เสร็จสิ้น)
  3. ห้องตรวจที่ต้องไปพบแพทย์
  4. จำนวนคิวก่อนหน้า (Waiting Position)
  5. เวลาโดยประมาณที่จะถึงคิว
  6. คำแนะนำสำหรับคนไข้

### ข้อ 50: ระบบคำนวณระยะเวลารอคิวโดยประมาณ (Estimated Wait Time) อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/QueueStatusView.tsx`
* **แนวคำตอบ:** คำนวณจากสูตร:
  $$\text{เวลารอคอย (นาที)} = \text{จำนวนคิวก่อนหน้า} \times \text{เวลาตรวจเฉลี่ยต่อคน (เช่น 10-15 นาที)}$$
  และนำมาบวกกับเวลาปัจจุบันเพื่อแสดงผลเป็นช่วงเวลาโดยประมาณ เช่น "ประมาณ 10:30 - 10:45 น."

### ข้อ 51: ระบบส่งเสียงแจ้งเตือนเมื่อใกล้ถึงคิวโดยใช้เทคโนโลยีใด?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueueNotification.ts`
* **แนวคำตอบ:** ใช้ **Web Audio API** สร้างคลื่นเสียงสังเคราะห์ (Synthesized 3-Tone Chime) ผ่าน AudioContext โดยไม่ต้องโหลดไฟล์เสียง .mp3 ภายนอก ทำให้ทำงานได้รวดเร็วและไม่เปลืองเน็ต

### ข้อ 52: ทำไมบางครั้งเสียงเตือนถึงไม่ดังเมื่อเปิดหน้าเว็บบนเบราว์เซอร์?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueueNotification.ts`
* **แนวคำตอบ:** เกิดจากนโยบาย **Autoplay Policy** ของเบราว์เซอร์ยุคใหม่ (เช่น Chrome/Safari) ที่จะบล็อกเสียงอัตโนมัติหากผู้ใช้ยังไม่ได้มีปฏิสัมพันธ์ (Interaction) หรือคลิกบนหน้าเว็บเลยแม้แต่ครั้งเดียว

### ข้อ 53: ระบบแก้ปัญหา Autoplay Policy ของเบราว์เซอร์อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueueNotification.ts`
* **แนวคำตอบ:** มีการดักจับการคลิกครั้งแรกของผู้ใช้เพื่อปลดล็อกสถานะ `AudioContext.resume()` ล่วงหน้า เตรียมพร้อมสำหรับการเล่นเสียงเตือนเมื่อสถานะคิวเปลี่ยนแปลง

### ข้อ 54: ระบบแจ้งเตือนคิวด้วยการสั่นบนมือถืออย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueueNotification.ts`
* **แนวคำตอบ:** ใช้คำสั่ง `navigator.vibrate([200, 100, 200])` ซึ่งเป็นการสั่นแบบเป็นจังหวะ เพื่อให้ผู้ป่วยรู้สึกตัวแม้เก็บโทรศัพท์ไว้ในกระเป๋าเสื้อ

### ข้อ 55: ระบบป้องกันการลงทะเบียนจองคิวซ้ำซ้อน (Duplicate Queue Guard) ทำงานอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/registration/RegistrationView.tsx`
* **แนวคำตอบ:** ก่อนส่งแบบฟอร์มจองคิว ระบบจะตรวจสอบก่อนว่าผู้ป่วยมีคิวที่ยังค้างอยู่ในสถานะรอตรวจหรือไม่ หากพบว่ามีอยู่แล้ว ระบบจะแสดงข้อความแจ้งเตือนและพาผู้ป่วยเปิดหน้าบัตรคิวเดิมทันที เพื่อป้องกันการกดคิวเบิ้ล

### ข้อ 56: ขั้นตอนการยกเลิกคิว (Cancel Queue) มีการป้องกันการกดผิดอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/QueueStatusView.tsx`
* **แนวคำตอบ:** ใช้การยืนยันแบบ **2-Step Confirmation Modal** โดยเมื่อกดปุ่ม "ยกเลิกคิว" ระบบจะเปิดป๊อปอัปแจ้งผลกระทบและขอให้ผู้ป่วยกดยืนยันอีกครั้ง จึงจะยิง API ไปยกเลิกคิวที่ระบบกลาง

### ข้อ 57: ผู้ป่วยสามารถบันทึกบัตรคิวเก็บไว้ในเครื่องได้อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/queue-card-canvas.ts`
* **แนวคำตอบ:** มีปุ่ม "บันทึกภาพบัตรคิว" ระบบจะใช้ **HTML5 Canvas API** วาดหมายเลขคิว ชื่อผู้ป่วย และ QR Code ออกมาเป็นภาพ `.png` คุณภาพสูง แล้วสั่งดาวน์โหลดลงอัลบั้มภาพในมือถือของผู้ป่วยโดยอัตโนมัติ

### ข้อ 58: ป้ายจอรวมของโรงพยาบาล (Live OPD Board) เชื่อมโยงกับหน้านี้อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/QueueStatusView.tsx`
* **แนวคำตอบ:** มีลิงก์เปิดไปยังหน้าจอแสดงผลรวมของโรงพยาบาล เพื่อให้ผู้ป่วยสามารถเทียบดูคิวของทุกห้องตรวจพร้อมกันได้

### ข้อ 59: เมื่อแพทย์กดเรียกคิวที่ห้องตรวจ หน้าจอคนไข้จะเปลี่ยนสถานะอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/QueueStatusView.tsx`, `useQueuePolling.ts`
* **แนวคำตอบ:** ในรอบ Polling ถัดไป (ภายใน 10 วินาที) ข้อมูลจาก Backend จะเปลี่ยนสถานะจาก "รอตรวจ" เป็น "กำลังตรวจ" หรือ "เชิญเข้าห้องตรวจ" พร้อมเปลี่ยนสีการ์ดคิวเป็นสีเขียวเด่นชัดและส่งเสียงเตือนทันที

### ข้อ 60: หากตรวจเสร็จสิ้นแล้ว บัตรคิวจะหายไปหรือแสดงอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/QueueStatusView.tsx`
* **แนวคำตอบ:** การ์ดจะเปลี่ยนเป็นสถานะ "ตรวจเสร็จสิ้น" พร้อมแสดงคำแนะนำขั้นตอนต่อไป เช่น ให้ไปรับยาที่ห้องการเงิน/เภสัชกรรม และมีปุ่มให้กดปิดเพื่อกลับสู่หน้าปกติ

### ข้อ 61: โค้ดส่วนใดที่จัดการเรื่อง Refresh Rate ของการ Polling?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/config/runtime-config.ts` (ฟิลด์ `statusRefreshMs`)
* **แนวคำตอบ:** ถูกกำหนดไว้ในอ็อบเจกต์ Config ซึ่งอ่านค่ามาจาก `window.PATIENT_APP_ENV.statusRefreshMs` ทำให้สามารถปรับความเร็ว-ช้าของการอัปเดตได้จากภายนอก

### ข้อ 62: หาก API คิวตอบกลับมาเป็น HTTP 404 หมายความว่าอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueuePolling.ts`
* **แนวคำตอบ:** หมายความว่า "ผู้ป่วยรายนี้ไม่มีคิวค้างอยู่ในระบบ" ระบบจะปรับค่า State คิวให้เป็น `null` เพื่อให้หน้าจอแสดงผลปุ่ม "ลงทะเบียนรับคิวใหม่"

### ข้อ 63: ฟังก์ชัน `clearActiveQueue` มีไว้ทำหน้าที่อะไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueuePolling.ts`
* **แนวคำตอบ:** มีไว้สำหรับล้างข้อมูลคิวออกจาก State ทันทีหลังจากที่ผู้ป่วยทำการกดยกเลิกคิวสำเร็จ เพื่อไม่ให้หน้าจอยังแสดงคิวเก่าค้างอยู่

---

## หมวดที่ 5: การลงทะเบียน, PDPA และเวชระเบียน (ข้อ 64 - 76)

### ข้อ 64: ระบบมีกลไกปฏิบัติตาม พ.ร.บ. คุ้มครองข้อมูลส่วนบุคคล (PDPA) อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/registration/PdpaConsentModal.tsx`
* **แนวคำตอบ:** มี **PDPA Consent Gate** ก่อนที่ผู้ป่วยจะเข้าถึงฟอร์มลงทะเบียน ระบบจะบังคับให้เปิดอ่านข้อกำหนดการจัดเก็บ การประมวลผล และการเปิดเผยข้อมูลสุขภาพ โดยต้องกดยินยอม (Consent) ก่อนเท่านั้น จึงจะอนุญาตให้กรอกข้อมูลได้

### ข้อ 65: ข้อมูลสุขภาพส่วนใดบ้างที่ถูกจัดเก็บในหน้าคัดกรองอาการ?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/patient-profile/PatientProfileForm.tsx`
* **แนวคำตอบ:** 
  1. อาการสำคัญที่มาโรงพยาบาล (Chief Complaint)
  2. โรคประจำตัว (Chronic Diseases)
  3. ประวัติการแพ้ยาและอาหาร (Allergies)
  4. ยาที่กำลังรับประทานอยู่เป็นประจำ (Current Medications)
  5. สัญญาณชีพเบื้องต้น (ถ้ามี)

### ข้อ 66: ฟิลด์ "เลขประจำตัวประชาชน 13 หลัก" มีการตรวจสอบความถูกต้อง (Validation) อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/registration/RegistrationView.tsx`
* **แนวคำตอบ:** ตรวจสอบว่าเป็นตัวเลขล้วน มีความยาวครบ 13 หลัก และสามารถเสริมอัลกอริทึมการตรวจสอบ Checksum (Mod 11) ของรหัสบัตรประชาชนไทยได้

### ข้อ 67: ข้อมูลติดต่อฉุกเฉิน (Emergency Contact) มีความสำคัญอย่างไรในระบบนี้?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/patient-profile/PatientProfileForm.tsx`
* **แนวคำตอบ:** บังคับให้ระบุชื่อ เบอร์โทรศัพท์ และความสัมพันธ์ (เช่น บิดา, มารดา, คู่สมรส) เพื่อให้เจ้าหน้าที่โรงพยาบาลสามารถติดต่อญาติได้ทันท่วงทีในกรณีที่ผู้ป่วยเกิดภาวะวิกฤตขณะรอตรวจ

### ข้อ 68: ในหน้า "บัญชีผู้ป่วย" (`AccountView`) มีแท็บข้อมูลอะไรบ้าง?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/account/AccountView.tsx`
* **แนวคำตอบ:** มี 2 แท็บหลัก:
  1. **แท็บรายการนัดหมายแพทย์ (Appointments):** ดูวัน เวลา คลินิก และแพทย์ที่นัด
  2. **แท็บบันทึกประวัติการรักษา (Medical Records):** ดูประวัติการพบแพทย์ในอดีต สัญญาณชีพ และผลวินิจฉัย

### ข้อ 69: ผู้ป่วยสามารถนำข้อมูลนัดหมายแพทย์ไปใส่ในปฏิทินมือถือได้อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/account/AccountView.tsx`
* **แนวคำตอบ:** มีปุ่ม **"ดาวน์โหลดไฟล์นัด (.ics)"** ซึ่งระบบจะสร้างไฟล์มาตรฐาน iCalendar (`.ics`) ขึ้นมา เมื่อกดเปิด โทรศัพท์จะนำนัดหมายนี้ไปบันทึกใน Google Calendar หรือ Apple Calendar โดยอัตโนมัติ

### ข้อ 70: หมายเลขประจำตัวผู้ป่วย (HN) ได้มาจากที่ใด?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/account/AccountView.tsx`, `mock_backend.py`
* **แนวคำตอบ:** ออกให้โดยระบบ Hospital Information System (HIS) ของโรงพยาบาลหลังจากการลงทะเบียนครั้งแรก (เช่น `HN-67001`)

### ข้อ 71: ข้อมูลสัญญาณชีพ (Vital Signs) ในประวัติการรักษามีค่าอะไรบ้าง?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/account/AccountView.tsx`
* **แนวคำตอบ:** ความดันโลหิต (Blood Pressure: Systolic/Diastolic), อัตราการเต้นของหัวใจ (Pulse), อุณหภูมิร่างกาย (Temp), และระดับออกซิเจนในเลือด (SpO2)

### ข้อ 72: ข้อมูลส่วนบุคคลของผู้ป่วยสามารถแก้ไขเองได้หรือไม่?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/patient-profile/PatientProfileForm.tsx`
* **แนวคำตอบ:** ข้อมูลบางประเภทแก้ไขได้เอง เช่น เบอร์โทรศัพท์ ที่อยู่ บุคคลติดต่อฉุกเฉิน และอาการแพ้ยา แต่ข้อมูลสำคัญทางกฎหมาย เช่น ชื่อ-นามสกุล และเลขประจำตัวประชาชน จะต้องติดต่อเจ้าหน้าที่เวชระเบียนพร้อมบัตรประชาชนตัวจริง

### ข้อ 73: ระบบป้องกันการกรอกข้อมูลไม่ครบถ้วนในฟอร์มลงทะเบียนอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/registration/RegistrationView.tsx`
* **แนวคำตอบ:** มีระบบ Form Validation ตรวจสอบฟิลด์บังคับ (`required`) หากผู้ใช้กรอกไม่ครบหรือผิดรูปแบบ ช่องนั้นจะแสดงกรอบสีแดงพร้อมข้อความแจ้งเตือน และปุ่มบันทึกจะถูกปิดการทำงาน (Disabled)

### ข้อ 74: ข้อมูล Consent PDPA ถูกส่งไปบันทึกที่ใด?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/registration/RegistrationView.tsx`, `src/shared/api/patient-api.ts`
* **แนวคำตอบ:** ถูกแนบไปพร้อมกับ Payload ของการลงทะเบียน เพื่อให้เซิร์ฟเวอร์ Backend บันทึก Timestamp และเวอร์ชันของนโยบายความเป็นส่วนตัวที่ผู้ป่วยกดยินยอมไว้เป็นหลักฐาน

### ข้อ 75: ถ้าผู้ป่วยเคยกดปฏิเสธ PDPA จะเกิดอะไรขึ้น?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/registration/PdpaConsentModal.tsx`
* **แนวคำตอบ:** ระบบจะปิด Modal แล้วกลับสู่หน้าก่อนหน้า โดยไม่อนุญาตให้เปิดแบบฟอร์มลงทะเบียนรับคิวได้ เนื่องจากตามกฎหมายโรงพยาบาลไม่สามารถประมวลผลข้อมูลการรักษาได้หากไม่ได้รับความยินยอม

### ข้อ 76: การดึงข้อมูลประวัติการรักษาผู้ป่วย ใช้ API Endpoint ใด?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/api/patient-api.ts`
* **แนวคำตอบ:** เรียกใช้ฟังก์ชัน `patientApi.profile(token)` ยิงไปที่ `/api/patient/me` และ `patientApi.history(token)` ยิงไปที่ `/api/patient/history`

---

## หมวดที่ 6: การออกแบบเพื่อผู้สูงอายุและ Responsive UX (ข้อ 77 - 85)

### ข้อ 77: ระบบรองรับการปรับขนาดตัวอักษรสำหรับผู้สูงอายุอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/settings/SettingsView.tsx`, `src/shared/ui/SiteShell.tsx`, `src/app/globals.css`
* **แนวคำตอบ:** มีฟังก์ชันเปลี่ยนขนาดตัวอักษร 3 ระดับ: `normal`, `large`, และ `xlarge` โดยระบบจะเซ็ตคุณสมบัติ `data-font-size` ไว้ที่ Element ครอบหลัก ทำให้ตัวแปร CSS Variable ของ `font-size` ทั้งระบบขยายขึ้นตามสัดส่วน

### ข้อ 78: เมื่อผู้สูงอายุเลือกขนาดตัวอักษรแล้ว ปิดเว็บแล้วเปิดใหม่ ค่าจะยังอยู่หรือไม่?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx` (ฟังก์ชัน `getInitialFontSize`), `SettingsView.tsx`
* **แนวคำตอบ:** ยังอยู่ครับ เพราะระบบจะบันทึกค่าลงใน `localStorage.setItem('app_font_size', size)` ทันทีที่มีการปรับเปลี่ยน

### ข้อ 79: การออกแบบปุ่มและจุดสัมผัส (Touch Targets) สำหรับผู้สูงอายุ อิงมาตรฐานอะไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/globals.css`, `README.md`
* **แนวคำตอบ:** ออกแบบตามมาตรฐาน **WCAG (Web Content Accessibility Guidelines)** โดยกำหนดขนาดปุ่มกดสัมผัสขั้นต่ำไม่น้อยกว่า **48x48 พิกเซล** เพื่อให้ผู้สูงอายุหรือผู้ที่มีปัญหาเรื่องกล้ามเนื้อมือสามารถกดได้ง่าย ไม่กดพลาด

### ข้อ 80: มีการวางฟังก์ชันเบอร์โทรฉุกเฉินไว้ในระบบอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/settings/SettingsView.tsx`, `src/shared/ui/AppNavbar.tsx`
* **แนวคำตอบ:** มีปุ่มโทรออกด่วนสายด่วนกู้ชีพ **1669** แบบกดครั้งเดียว (One-Touch Dialing) โดยใช้โปรโตคอล `tel:1669` ซึ่งจะเปิดแอปพลิเคชันโทรศัพท์ของมือถือขึ้นมาทันที

### ข้อ 81: ความแตกต่างของ Layout ระหว่างมือถือและคอมพิวเตอร์เป็นอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/shared/ui/SiteShell.tsx`, `src/app/globals.css`
* **แนวคำตอบ:** 
  * **มือถือ (Mobile Viewport):** จัดวางเป็น Single-column ไหลลงมาตามแนวดิ่ง เมนูนำทางอยู่ด้านล่างหน้าจอ
  * **คอมพิวเตอร์ (Desktop Viewport):** จัดวางเป็น Multi-column เมนูนำทางอยู่แถบด้านบนขวา ใช้พื้นที่หน้าจอคุ้มค่า

### ข้อ 82: โทนสีและ Contrast ของระบบถูกออกแบบอย่างไรให้เหมาะกับโรงพยาบาล?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/globals.css`
* **แนวคำตอบ:** ใช้โทนสีเขียวการแพทย์ (Medical Teal/Emerald) ให้ความรู้สึกสะอาด สบายตา และปลอดภัย และมีค่าอัตราส่วนความต่างของสี (Contrast Ratio) ผ่านเกณฑ์ระดับ AA ของ WCAG อ่านง่ายแม้อยู่กลางแจ้ง

### ข้อ 83: ในหน้าแป้นพิมพ์ PIN สำหรับผู้สูงอายุ มีข้อความแนะนำอย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/auth/PinAuthView.tsx`
* **แนวคำตอบ:** มีข้อความอธิบายสถานะชัดเจน เช่น "กรุณากรอกรหัส PIN 6 หลักเพื่อความปลอดภัย" พร้อมแสดงจุดวงกลม 6 จุดระบุความคืบหน้าการกรอก ทำให้เข้าใจง่ายโดยไม่ต้องมีคนสอน

### ข้อ 84: ฟอนต์ตัวอักษรที่ใช้ในระบบคือฟอนต์อะไร เพราะเหตุใด?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/globals.css`
* **แนวคำตอบ:** ใช้ฟอนต์ระบบสไตล์ Sans-Serif ภาษาไทยที่อ่านง่ายและคมชัด (เช่น Prompt, Sarabun หรือ San Francisco/Segoe UI) เพื่อให้สระและวรรณยุกต์ไม่ซ้อนทับกัน

### ข้อ 85: ถ้าผู้สูงอายุเผลอกดปุ่ม Back ของโทรศัพท์ หน้าเว็บจะหลุดหรือไม่?
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/page.tsx`
* **แนวคำตอบ:** ไม่หลุดครับ เพราะระบบมีการดักจับสถานะผ่าน `history.pushState` และจำค่า `view` ปัจจุบันไว้ใน Storage ทำให้ยังคงอยู่ในหน้าจอการทำงานเดิม

---

## หมวดที่ 7: การทดสอบอัตโนมัติและการส่งมอบระบบ (ข้อ 86 - 93)

### ข้อ 86: ระบบนี้ใช้เครื่องมือใดในการทำ Automated End-to-End (E2E) Testing?
* **ไฟล์ที่เกี่ยวข้อง:** `playwright.config.ts`, `package.json`
* **แนวคำตอบ:** ใช้ **Playwright** ซึ่งเป็นเฟรมเวิร์กทดสอบเว็บแอปพลิเคชันระดับมืออาชีพ สามารถควบคุมเบราว์เซอร์ Chromium, Firefox และ WebKit ได้อย่างแม่นยำ

### ข้อ 87: ชุดทดสอบ E2E ครอบคลุมการทดสอบทั้งหมดกี่กรณี (Test Cases)?
* **ไฟล์ที่เกี่ยวข้อง:** `tests/e2e/`, `README.md`
* **แนวคำตอบ:** ครอบคลุม **32 กรณีทดสอบ (100% Pass Rate)** ทั้งบน Desktop และ Mobile Emulation (เช่น Pixel 7)

### ข้อ 88: ไฟล์ทดสอบใน `tests/e2e/` แบ่งออกเป็นไฟล์อะไรบ้าง?
* **ไฟล์ที่เกี่ยวข้อง:** `tests/e2e/`
* **แนวคำตอบ:** แบ่งเป็น 3 ไฟล์หลัก:
  1. `login.spec.ts`: ทดสอบระบบ Login, Validation, การสลับดูรหัสผ่าน
  2. `registration.spec.ts`: ทดสอบการยอมรับ PDPA และการกรอกฟอร์มลงทะเบียน
  3. `auth-and-queue.spec.ts`: ทดสอบลูปการทำงานตั้งแต่ Login -> ยืนยัน PIN -> รับบัตรคิว -> ยกเลิกคิว

### ข้อ 89: คำสั่งสำหรับสั่งรัน Playwright Test ทั้งหมดคือคำสั่งใด?
* **ไฟล์ที่เกี่ยวข้อง:** `package.json`
* **แนวคำตอบ:** 
  ```powershell
  npx playwright test
  ```
  หรือ `npm run test:e2e`

### ข้อ 90: หากต้องการเปิดดูผลการทดสอบแบบมีหน้าต่างกราฟิก (HTML Report) ใช้คำสั่งใด?
* **ไฟล์ที่เกี่ยวข้อง:** `package.json`
* **แนวคำตอบ:**
  ```powershell
  npx playwright show-report
  ```

### ข้อ 91: ในการทดสอบ Unit Test ฝั่ง Component ใช้เครื่องมืออะไร?
* **ไฟล์ที่เกี่ยวข้อง:** `vitest.config.mts`, `package.json`
* **แนวคำตอบ:** ใช้ **Vitest** ร่วมกับ **React Testing Library** และ **jsdom** สำหรับทดสอบฟังก์ชันการทำงานของแต่ละ Component และ Custom Hooks แบบแยกเดี่ยว

### ข้อ 92: การทดสอบประเภท E2E แตกต่างจาก Unit Test อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `tests/e2e/`, `src/features/queue/QueueStatusView.test.tsx`
* **แนวคำตอบ:** 
  * **Unit Test:** ทดสอบเฉพาะฟังก์ชันหรือ Component ย่อยๆ โดยตัดขาดจากส่วนอื่น (ใช้เวลาเร็วมาก)
  * **E2E Test:** ทดสอบเสมือนมีผู้ใช้จริงมานั่งคลิกหน้าจอตั้งแต่เปิดเว็บ กรอกฟอร์ม ยิง API และตรวจผลลัพธ์ทั้งระบบร่วมกัน

### ข้อ 93: ก่อนนำระบบขึ้น Production มีคำสั่งตรวจสอบ Type และ Lint อย่างไร?
* **ไฟล์ที่เกี่ยวข้อง:** `package.json`
* **แนวคำตอบ:**
  ```powershell
  npm run typecheck   # ตรวจสอบ TypeScript ทั้งโปรเจกต์
  npm run lint        # ตรวจสอบไวยากรณ์และความสะอาดของโค้ดด้วย ESLint
  ```

---

## หมวดที่ 8: คำถามจำลองสถานการณ์ "สั่งแก้โค้ดสด / ย้ายปุ่ม / ปรับเงื่อนไข" (ข้อ 94 - 100)

### ข้อ 94: "ถ้ากรรมการสั่งให้เปลี่ยนความถี่ในการอัปเดตคิวจาก 10 วินาที เป็น 5 วินาที ต้องแก้ที่ไหน?"
* **ไฟล์ที่เกี่ยวข้อง:** `public/runtime-config.js` และ `src/shared/config/runtime-config.ts`
* **แนวทางการแก้สด:**
  1. เปิดไฟล์ `public/runtime-config.js`
  2. แก้ไขค่า `statusRefreshMs: 10000` เปลี่ยนเป็น `statusRefreshMs: 5000`
  3. เซฟไฟล์และรีเฟรชหน้าจอได้ทันทีโดยไม่ต้องรัน Build ใหม่

### ข้อ 95: "ถ้ากรรมการสั่งให้เพิ่มช่องกรอก 'กรุ๊ปเลือด' ในหน้าลงทะเบียน ต้องแก้ไฟล์ใดบ้าง?"
* **ไฟล์ที่เกี่ยวข้อง:**
  1. UI หน้าบ้าน: `src/features/registration/RegistrationView.tsx` (เพิ่มแท็ก `<select>` หรือ `<input>`)
  2. Interface ข้อมูล: `src/shared/api/types.ts` (เพิ่มฟิลด์ `blood_type?: string` ใน Request Type)
  3. ตัวส่ง API: `src/shared/api/patient-api.ts`
  4. Backend: `mock_backend.py` (รับค่าและบันทึกลง Profile)

### ข้อ 96: "ถ้ากรรมการสั่งให้ย้ายปุ่ม 'ยกเลิกคิว' ไปไว้ด้านบนสุดของการ์ดคิว ต้องทำอย่างไร?"
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/QueueStatusView.tsx`
* **แนวทางการแก้สด:**
  1. เปิดไฟล์ `src/features/queue/QueueStatusView.tsx`
  2. ค้นหาบล็อก JSX `<button ... onClick={() => setShowCancelModal(true)}>`
  3. Cut โค้ดบล็อกนั้น ย้ายขึ้นไปวางไว้ใต้แท็ก `<h2>` หัวข้อหมายเลขคิว
  4. บันทึกไฟล์ Hot Reload จะอัปเดตตำแหน่งปุ่มทันที

### ข้อ 97: "ถ้ากรรมการสั่งให้ปิดเสียงแจ้งเตือนกระดิ่งคิวชั่วคราว ต้องแก้ตรงไหน?"
* **ไฟล์ที่เกี่ยวข้อง:** `src/features/queue/useQueueNotification.ts` หรือ `QueueStatusView.tsx`
* **แนวทางการแก้สด:**
  1. เปิดไฟล์ `src/features/queue/useQueueNotification.ts`
  2. ในฟังก์ชัน `playChime()` ให้ใส่ `return;` ไว้ที่บรรทัดแรกสุดของฟังก์ชันเพื่อหยุดการสร้างเสียงสังเคราะห์

### ข้อ 98: "ถ้ากรรมการสั่งให้เปลี่ยนรหัส PIN สำหรับบัญชีทดสอบจาก 123456 เป็น 999999 ทำอย่างไร?"
* **ไฟล์ที่เกี่ยวข้อง:** `mock_backend.py` (หรือเปลี่ยนผ่านหน้าเว็บ)
* **แนวทางการแก้สด:**
  * **วิธีที่ 1 (ผ่าน UI สด):** กดเข้าเมนู "ตั้งค่า" -> เลือก "เปลี่ยนรหัส PIN" -> กรอกรหัสเดิม 123456 และตั้งรหัสใหม่ 999999 (วิธีนี้ดูเป็นมืออาชีพที่สุด)
  * **วิธีที่ 2 (ในโค้ด Mock):** ไปที่ `mock_backend.py` ค้นหา `MOCK_DATABASE_SECURITY` แล้วแก้ไขค่ารหัสที่เก็บไว้

### ข้อ 99: "ถ้ากรรมการสั่งให้เปลี่ยนสีธีมหลักของปุ่มจากสีเขียวเป็นสีน้ำเงิน ต้องแก้ที่ไหน?"
* **ไฟล์ที่เกี่ยวข้อง:** `src/app/globals.css`
* **แนวทางการแก้สด:**
  1. เปิดไฟล์ `src/app/globals.css`
  2. ค้นหาส่วน `:root` ที่กำหนด CSS Variable เช่น `--primary`, `--primary-hover`
  3. เปลี่ยนรหัสสี Hex จากสีเขียว (เช่น `#0d9488` หรือ `#10b981`) เป็นโทนสีน้ำเงิน (เช่น `#2563eb`)
  4. ปุ่มและแถบสีทั้งระบบจะเปลี่ยนตามตัวแปรทันที

### ข้อ 100: "ถ้าเกิดกรณีเลวร้ายที่สุด กรรมการสั่งแก้ระบบแล้วเราทำไม่เป็นหรือจำ Syntax ไม่ได้ ควรพูดยังไง?"
* **แนวคำตอบเพื่อเอาตัวรอดอย่างสง่างาม:**
  > *"สำหรับการปรับเปลี่ยนส่วนนี้ สามารถทำได้โดยหลักการคือ 1) เข้าไปจัดการที่ Component `[ระบุชื่อไฟล์]` เพื่อดึง State ของข้อมูลตัวนี้ออกมา 2) ทำการส่ง Callback Props ไปยัง Component ลูก และ 3) ตรวจสอบเงื่อนไขใน API Client ครับ แต่เนื่องจากในเวลาสอบมีจำกัดและกลัวกระทบโครงสร้างชุดทดสอบ 32 เคส ขออนุญาตบันทึกเป็นข้อเสนอแนะเพื่อนำไปปรับปรุงในเวอร์ชันถัดไปครับ"*

---
**จัดทำขึ้นเพื่อ:** การเตรียมความพร้อมสอบป้องกันโครงงานวิศวกรรม/ปริญญานิพนธ์  
**ขอให้โชคดีและประสบความสำเร็จในการสอบครับ!** 🏆
