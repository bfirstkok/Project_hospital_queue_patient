{
  "name": "hospital-queue-patient-portal",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check config.js && node --check scripts/build.mjs",
    "build": "node scripts/build.mjs"
  }
}
